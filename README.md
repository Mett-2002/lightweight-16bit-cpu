# lightweight-16bit-cpu
A lightweight modular 16-bit CPU built in Verilog, featuring a structured datapath (registers, ALU, buses), an FSM-based control unit (FETCH/DECODE/INDIRECT/EXECUTE), and a memory with arithmetic subroutines. Includes a full testbench verifying add, sub, and multiply operations.
